Flask==2.2.5
Flask-Cors==4.0.2
Flask-SQLAlchemy==3.0.3
Flask-Migrate==3.1.0
Flask-JWT-Extended==4.4.4
Flask-Reuploaded==1.2.0
Werkzeug==3.0.6
itsdangerous==2.1.2
click==8.1.3
greenlet==2.0.2
python-dotenv==0.21.1
# Uncomment if using Postgres in production; remove for SQLite ephemeral testing
# psycopg2-binary==2.9.9
# Admin interface (optional)
# Flask-Admin==1.6.1
# Heavy / excluded in serverless: ortools, WeasyPrint, gevent, locust, pytest
# Scheduling & PDF generation features will return an explanatory error if invoked.
